# Cursor Workflow

1. Plan mode: requirements → architecture → phased implementation plan
2. Agent mode: archetype bootstrap → core API → HTL UI → tests → docs
3. AEM skills: `ensure-agents-md`, `create-component` servlet/HTL conventions
4. Validate with `mvn clean install -pl ../../tests -am`
5. Deploy with `mvn clean install -PautoInstallSinglePackage` when SDK available
